## Executando a aplicação

```
$ cd api-my-account
$ npm install
$ npm run dev
$ npm install -g typeorm
$ npm run typeorm migration:run

criando migration
$ npm run typeorm migration:run
npx typeorm migration:create -n CreateMonthlyBudget


```